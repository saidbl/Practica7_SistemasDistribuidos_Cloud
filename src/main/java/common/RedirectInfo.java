package common;

public class RedirectInfo {
        final String host;
        final int port;

        RedirectInfo(String host, int port) {
            this.host = host;
            this.port = port;
        }
 }